#include <stdio.h>

int main() {
    int num, sum = 0;
    scanf("%d", &num);
    for (int i = 1; i <= num; i++) {
        int fact = 1;
        for (int j = 1; j <= i; j++)
            fact *= j;
        sum += fact;
    }
    printf("%d\n", sum);
    return 0;
}
