#include <stdio.h>

int main() {
    int a = 8, b = 6;
    char ch;
    scanf(" %c", &ch);
    switch (ch) {
        case '+': printf("%d", a + b); break;
        case '-': printf("%d", a - b); break;
        case '*': printf("%d", a * b); break;
        default: printf("Invalid operator");
    }
    return 0;
}
