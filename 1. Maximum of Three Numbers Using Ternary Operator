#include <stdio.h>

int main() {
    int a = 5, b = 8, c = 3, d = 10;

    int max = (a > b) ? ((a > c) ? ((a > d) ? a : d) : ((c > d) ? c : d)) 
                      : ((b > c) ? ((b > d) ? b : d) : ((c > d) ? c : d));

    printf("Max: %d\n", max);
    return 0;
}
